syntax = "proto3";

package cart;

service cartService { 
  rpc sendProduct (ProductID) returns (Response) {}
}

message ProductID {
  string id = 1;
}

message Response {
  int32 code = 1;
  string message = 2;
}